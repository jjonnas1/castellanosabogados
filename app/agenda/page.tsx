// app/agenda/page.tsx
export default function AgendaPage() {
  return (
    <main
      style={{
        padding: "48px",
        color: "#fff",
        background: "#0b1220",
        minHeight: "100vh",
        fontFamily: "system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Cantarell",
      }}
    >
      <h1 style={{ fontSize: 28, marginBottom: 8 }}>CastellanosAbogados — Agenda</h1>
      <p>Si ves este texto, la ruta /agenda funciona y el problema estaba en el componente anterior.</p>
    </main>
  );
}
